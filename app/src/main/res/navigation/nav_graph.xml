<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:id="@+id/nav_graph"
  app:startDestination="@id/simulation_fragment">

  <fragment
    android:id="@+id/simulation_fragment"
    android:label="@string/simulation_label"
    android:name="edu.cnm.deepdive.crapssimulator.controller.CrapsFragment"
    tools:layout="@layout/fragment_craps">

    <action
      android:id="@+id/open_settings"
      app:destination="@id/settings_fragment"
      app:popUpTo="@id/simulation_fragment"/>

  </fragment>

  <fragment
    android:id="@+id/settings_fragment"
    android:label="@string/settings_label"
    android:name="edu.cnm.deepdive.crapssimulator.controller.SettingsFragment"/>

</navigation>